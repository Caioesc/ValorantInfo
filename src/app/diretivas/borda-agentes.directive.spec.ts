import { BordaAgentesDirective } from './borda-agentes.directive';

describe('BordaAgentesDirective', () => {
  it('should create an instance', () => {
    const directive = new BordaAgentesDirective();
    expect(directive).toBeTruthy();
  });
});
