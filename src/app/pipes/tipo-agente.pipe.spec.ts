import { TipoAgentePipe } from './tipo-agente.pipe';

describe('TipoAgentePipe', () => {
  it('create an instance', () => {
    const pipe = new TipoAgentePipe();
    expect(pipe).toBeTruthy();
  });
});
